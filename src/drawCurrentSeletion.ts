
/**
 * Draws the current selection in the DOM
 * @param selection Selection to draw
 */
const drawCurrentSeleciton = (selection: Range | null): void => {
  // TODO: draw the rectagles in a DOM container
  // eslint-disable-next-line no-console
  console.log(selection);
};

export default drawCurrentSeleciton;